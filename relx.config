%% -*- mode: Erlang; fill-column: 80; comment-column: 75; -*-
{release, {minasan, "0.0.1"},
 [minasan]}.

{dev_mode, true}.
{include_erts, false}.

{extended_start_script, true}.

{sys_config, "config/sys.config"}.

{overlay, [{copy, "Procfile", "Procfile"}]}.
